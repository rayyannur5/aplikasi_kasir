import 'package:flutter/material.dart';

class LaporanTransaksiDetailPage extends StatelessWidget {
  const LaporanTransaksiDetailPage({super.key, required this.id});
  final String id;

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
